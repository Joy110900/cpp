#include <iostream>

using namespace std;    //Add this to evry code for functions first find in std lib and then find in another libs. 

int main()
{
    cout << "Hello World";
    std:: cout << "Hey";    //std:: means to look for the func 1st in the standard lib and then in other libs. We can write any lib name instead of std so the func is first searched in that lib and then in other lib. 
}